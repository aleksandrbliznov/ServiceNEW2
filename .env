# Flask Configuration
SECRET_KEY=dev-secret-key-change-in-production
FLASK_ENV=development

# Database Configuration
# SQLite (default, but has issues on some Windows systems)
SQLALCHEMY_DATABASE_URI=sqlite:///service_app.db

# PostgreSQL (recommended for production)
# SQLALCHEMY_DATABASE_URI=postgresql://username:password@localhost:5432/service_pro

# MySQL (alternative option)
# SQLALCHEMY_DATABASE_URI=mysql://username:password@localhost:3306/service_pro

# Email Configuration (Replace with your actual credentials)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False
MAIL_USERNAME=handyguru.eesti@gmail.com
MAIL_PASSWORD=lvgg yypl ukph awdj
MAIL_DEFAULT_SENDER=handyguru.eesti@gmail.com

# Upload Configuration
UPLOAD_FOLDER=static/uploads

# Babel Configuration
BABEL_DEFAULT_LOCALE=et
BABEL_SUPPORTED_LOCALES=et,en